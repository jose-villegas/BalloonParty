name: Deploy Doxygen to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'Assets/Source/**'
      - 'Docs/diagrams/**'
      - 'Docs/doxygen-custom.css'
      - 'Docs/doxygen-header.html'
      - 'Docs/doxygen-footer.html'
      - 'Doxyfile'
      - '.github/workflows/doxygen.yml'
  workflow_dispatch:

# Publishes by pushing the gh-pages branch, so Pages must be sourced from that branch
# rather than from an Actions artifact.
permissions:
  contents: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install Doxygen & Graphviz
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y graphviz
          # Install Doxygen 1.13.2 (latest stable binary) to avoid template mismatches
          wget -q https://github.com/doxygen/doxygen/releases/download/Release_1_13_2/doxygen-1.13.2.linux.bin.tar.gz
          tar xzf doxygen-1.13.2.linux.bin.tar.gz
          sudo cp doxygen-1.13.2/bin/doxygen /usr/local/bin/doxygen
          doxygen --version

      - name: Render Graphviz diagrams
        run: |
          cd Docs/diagrams
          for f in *.dot; do
            dot -Tsvg "$f" -o "${f%.dot}.svg"
          done

      - name: Generate Doxygen
        run: doxygen Doxyfile

      # Docs are the only occupant of the branch, so the publish replaces its contents
      # outright — deleted pages get pruned instead of lingering on the live site.
      - name: Publish to gh-pages root
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: Docs/doxygen/html
          commit_message: 'Deploy Doxygen from ${{ github.sha }}'
