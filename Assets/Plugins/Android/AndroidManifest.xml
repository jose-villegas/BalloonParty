<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- appCategory=game registers with Android's GameManager (game-optimized scheduling on
         Pixel-class devices; SurfaceFlinger reported gameMode=Unsupported without it). -->
    <application android:appCategory="game">
        <!-- Application Entry is Activity (androidApplicationEntry: 1) — the template's
             GameActivity block is removed: its BaseUnityGameActivityTheme resource only
             exists when the GameActivity entry point is enabled and fails AAPT otherwise. -->
        <activity android:name="com.unity3d.player.UnityPlayerActivity"
                  android:theme="@style/UnityThemeSelector">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="unityplayer.UnityActivity" android:value="true" />
        </activity>
    </application>
</manifest>
