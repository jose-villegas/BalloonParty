namespace BalloonParty.Game.Telemetry
{
    // The three fixed dimension axes (R5). A fourth is explicitly out of scope — see the catalog.
    internal enum MetricAxis
    {
        Color,
        BalloonType,
        ItemType
    }
}
