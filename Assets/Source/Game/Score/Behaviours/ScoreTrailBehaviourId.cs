namespace BalloonParty.Game.Score.Behaviours
{
    /// <summary>Identifies a score-trail choreography handler; the resolver maps a group's points to one.</summary>
    internal enum ScoreTrailBehaviourId
    {
        DefaultScore,
        BigScore,
    }
}
