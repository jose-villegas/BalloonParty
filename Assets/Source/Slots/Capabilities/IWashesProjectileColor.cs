namespace BalloonParty.Slots.Capabilities
{
    /// <summary>Marks a balloon that "washes" a projectile on contact — resetting its stolen colour back to none, like a fresh launch (the soap-bubble cluster).</summary>
    public interface IWashesProjectileColor
    {
    }
}
