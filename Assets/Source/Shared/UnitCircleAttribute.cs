using UnityEngine;

namespace BalloonParty.Shared
{
    /// <summary>Draws a Vector2 field as a unit-circle direction picker: click or drag inside the
    /// disc to aim the vector (written back normalized). The numeric field stays editable for exact
    /// values. Editor-side implementation: <c>UnitCircleDrawer</c>.</summary>
    public class UnitCircleAttribute : PropertyAttribute
    {
    }
}
