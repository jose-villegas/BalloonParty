namespace BalloonParty.Shared.Messages
{
    /// <summary>Published by the Ascent when it has fully settled, so the level-up phase returns to
    /// Playing and scoring reopens.</summary>
    public readonly struct LevelTransitionCompletedMessage
    {
    }
}
