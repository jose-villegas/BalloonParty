namespace BalloonParty.Shared.Messages
{
    /// <summary>Published the moment a loaded projectile is released and starts flying (see ThrowerController.TryFire).</summary>
    public readonly struct ProjectileFiredMessage
    {
    }
}
