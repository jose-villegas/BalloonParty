namespace BalloonParty.Projectile.Model
{
    /// <summary>Identifies a projectile buff type. Each buff is just an ID + a factor; the consumer defines what the factor means.</summary>
    public enum ProjectileBuffId
    {
        Speed,
        RainbowShield
    }
}
